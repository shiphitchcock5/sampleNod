language: node_js

# Version number
node_js:
  - 0.10
  

addons:
  firefox: "27.0"
  
services:
 - mysql
 - postgres
 - rabbitmq
 - selenium
 - rethinkdb
 - couchdb
 - neo4j
 

build:
  ci:
    - node --version
    
  
